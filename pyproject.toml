[build-system]
requires = ["flit-core==4.0.2"]
build-backend = "flit_core.buildapi"

[project]
name = "personal-feedstock"
version = "0.1.0"
description = "Add your description here"
readme = "readme.md"
requires-python = "==3.14.7"
dependencies = [
  "click==8.5.0",
  "click==8.5.0",
  "dprint-py==0.51.1.0",
  "httpx2==2.12.0",
  "jmespath==1.1.0",
  "pyyaml==6.0.3",
]

[tool.flit.module]
name = 'scripts'

[tool.ruff.lint]
extend-select = ['I']
