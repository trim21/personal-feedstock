[build-system]
requires = ["flit-core==3.12.0"]
build-backend = "flit_core.buildapi"

[project]
name = "personal-feedstock"
version = "0.1.0"
description = "Add your description here"
readme = "readme.md"
requires-python = "==3.14.2"
dependencies = [
  "click==8.3.1",
  "click==8.3.1",
  "dprint-py==0.51.1.0",
  "httpx2==2.12.0",
  "jmespath==1.1.0",
  "pyyaml==6.0.3",
]

[tool.flit.module]
name = 'scripts'

[tool.ruff.lint]
extend-select = ['I']
